*Guide on how to run SIMP code examples*

1. Run runtests.bat in the command prompt. (must be within this directory)
2. To run example separately use this command with the number being the example number
        java -classpath ./bin Sili < ./examples/test01.simp
	